# No environment variables are required for the core app.
# The app is offline-first. Optional online features may require user-provided credentials.

# Optional release signing variables. Do not commit real keystores or passwords.
# KEYSTORE_PATH=/absolute/path/to/upload.jks
# STORE_PASSWORD=change-me
# KEY_PASSWORD=change-me
