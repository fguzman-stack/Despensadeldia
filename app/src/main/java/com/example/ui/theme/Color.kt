package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// ─── Paleta Jardín de Frescura ──────────────────────────────────
// Verde fresco — disponible, saludable, consumible
val Emerald = Color(0xFF62C99A)
val EmeraldLight = Color(0xFF8ED6A2)
val EmeraldDark = Color(0xFF2E7D5B)

// Ámbar — atención sin alarma
val Amber = Color(0xFFF6C76D)
val AmberLight = Color(0xFFFFF0CB)
val AmberDark = Color(0xFFB8860B)

// Coral — urgencia, atención
val Coral = Color(0xFFF1846B)
val CoralLight = Color(0xFFFCE0D8)
val CoralDark = Color(0xFFCD5C4D)

// Lavanda (Sky) — donación, acción secundaria positiva
val Sky = Color(0xFFB8A4E8)
val SkyLight = Color(0xFFD4C7F5)

// ─── Fondos y Superficies — Oscuro ──────────────────────────────
val DarkBackground = Color(0xFF101814)
val DarkSurface = Color(0xFF18231D)
val DarkSurfaceVariant = Color(0xFF203027)

// ─── Fondos y Superficies — Claro ───────────────────────────────
val Cream = Color(0xFFF7F5EE)
val CreamWarm = Color(0xFFF0EDE0)
val Stone = Color(0xFFE8E5DE)
val StoneLight = Color(0xFFF2F0E9)

// ─── Contenedores con significado ──────────────────────────────
val GreenContainer = Color(0xFFDDF1DF)
val AmberContainer = Color(0xFFFFF0CB)
val CoralContainer = Color(0xFFFCE0D8)
val SkyContainer = Color(0xFFE8E0FA)

// ─── Texto — Oscuro ────────────────────────────────────────────
val TextOnDark = Color(0xFFF2F5EF)
val TextOnDarkSecondary = Color(0xFFB8C2B9)

// ─── Texto — Claro ─────────────────────────────────────────────
val TextPrimary = Color(0xFF1A241E)
val TextSecondary = Color(0xFF5A6B5E)
val TextTertiary = Color(0xFF8FA093)

// ─── Bordes y Líneas ──────────────────────────────────────────
val BorderDark = Color(0xFF314137)
val BorderMedium = Color(0xFF4A5A4E)
val BorderLight = Color(0xFFD6D3CD)

// ─── Tema Frutas en Fiesta (Fruit Pop) ────────────────────
val PremiumBackground = Color(0xFF1A0F14)           // Deep berry
val PremiumSurface = Color(0xFF2A1A22)              // Dark plum
val PremiumSurfaceVariant = Color(0xFF3D2430)       // Berry variant
val PremiumBorder = Color(0xFF5A2A3A)               // Berry border
val PremiumTextPrimary = Color(0xFFFFF0F0)          // Soft pink-white
val PremiumTextSecondary = Color(0xFFE8CCD8)        // Mauve claro (mejor contraste)
val PremiumAction = Color(0xFFFF6B81)               // Watermelon pink
val PremiumAttention = Color(0xFFF9CA24)            // Pineapple yellow
val PremiumUrgency = Color(0xFFD63031)              // Strawberry red
val PremiumDonation = Color(0xFF7BC67E)             // Kiwi green
val PremiumAccent = Color(0xFFFF9F43)               // Mango orange
